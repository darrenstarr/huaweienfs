# Pin for `scripts/fetch-vendor-ubuntu.sh ubuntu-7.0`.
#
# The fetch script reads LINUX_SOURCE_PKG, locates
# /usr/src/${LINUX_SOURCE_PKG}.tar.{bz2,xz,gz} (provided by the
# matching apt package), extracts it to a cache, and copies the
# paths in MANIFEST into this directory.
#
# Source: Ubuntu 26.04 LTS GA kernel.
# Install with:  sudo apt install linux-source-7.0.0
# (Only available on Ubuntu 26.04 / "resolute".)

LINUX_SOURCE_PKG=linux-source-7.0.0
VENDORED=2026-05-04
